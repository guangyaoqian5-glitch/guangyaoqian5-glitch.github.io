# Project Rules
Read docs/PRODUCT_SPEC.md before changing behavior. Preserve React + Vite and the Sites identity.
Do not invent personal facts. Run typecheck and build for changes. Inspect the running UI when possible; user confirms visual acceptance.
Never commit secrets or .env files.
